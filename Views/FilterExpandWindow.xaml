<Window x:Class="ActiveScanner.Views.FilterExpandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Filter"
        Height="520" Width="460"
        MinHeight="360" MinWidth="340"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <materialDesign:PackIcon Kind="Filter" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock x:Name="HeaderText"
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       FontWeight="Medium" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Match mode -->
        <StackPanel x:Name="ModePanel" Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,8">
            <TextBlock Text="Match:" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <ComboBox x:Name="ModeCombo" MinWidth="120"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
            <TextBlock Text="Tip: use &quot;Equals&quot; for pasted lists of exact names."
                       VerticalAlignment="Center" Margin="12,0,0,0" FontSize="11"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Editor -->
        <TextBox x:Name="EditorBox" Grid.Row="2"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="One value per line — paste an Excel column here"
                 AcceptsReturn="True" AcceptsTab="False"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalContentAlignment="Top"
                 FontFamily="Consolas" FontSize="12"
                 Padding="8"/>

        <!-- Info line -->
        <TextBlock Grid.Row="3" Margin="0,8,0,0" FontSize="11" TextWrapping="Wrap"
                   Foreground="{DynamicResource MaterialDesignBodyLight}">
            Each line is an OR term — a row matches if it matches any line. The legacy <Run Text="||" FontFamily="Consolas"/> separator still works. Changes take effect when you click <Run Text="Apply" FontWeight="SemiBold"/>; blank lines are removed.
        </TextBlock>

        <!-- Footer -->
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ValueCountText" Grid.Column="0" VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <TextBlock x:Name="RowCountText" Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            <Button Grid.Column="3" Content="Close" Click="Close_Click" IsCancel="True"
                    Style="{StaticResource MaterialDesignFlatButton}" MinWidth="80"/>
            <Button Grid.Column="4" Content="Apply" Click="Apply_Click" IsDefault="True" Margin="8,0,0,0"
                    Style="{StaticResource MaterialDesignRaisedButton}" MinWidth="80"/>
        </Grid>
    </Grid>
</Window>
