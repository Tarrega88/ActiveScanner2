<Window x:Class="ActiveScanner.Views.StaleDescriptionGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Create Group: Stale Descriptions"
        Height="490" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   Text="Adds selected computers whose Description does NOT contain their Last User to a new computer group."/>

        <CheckBox Grid.Row="1"
                  Content="Skip computers with no last-user data (recommended)"
                  IsChecked="{Binding SkipPlaceholderLastUsers}"
                  Margin="0,4,0,4"
                  ToolTip="When checked, computers without a queried last user (empty, &quot;Querying...&quot;, &quot;(Offline)&quot;, etc.) are excluded. When unchecked, they are also added to the group."/>

        <CheckBox Grid.Row="2"
                  Content="Skip computers with blank descriptions"
                  IsChecked="{Binding SkipBlankDescriptions}"
                  Margin="0,0,0,8"
                  ToolTip="When checked, computers whose Description is empty or whitespace are excluded from the group."/>

        <Border Grid.Row="3"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,4,0,12">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Margin="0,0,0,4" Text="Preview"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Selected computers"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedCount}" FontWeight="SemiBold"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Will be added (stale)" Foreground="#4CAF50"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MatchCount}" FontWeight="SemiBold" Foreground="#4CAF50"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Skipped: description already contains last user" Opacity="0.7"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SkippedDescriptionMatchesCount}" Opacity="0.7"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Skipped: no last-user data" Opacity="0.7"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SkippedNoLastUserCount}" Opacity="0.7"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Skipped: blank description" Opacity="0.7"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SkippedBlankDescriptionCount}" Opacity="0.7"/>
                </Grid>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="4" Text="Group name:" Margin="0,0,0,4"/>

        <TextBox Grid.Row="5"
                 x:Name="GroupNameTextBox"
                 materialDesign:HintAssist.Hint="Group Name"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="6"
                   Foreground="#E53935"
                   TextWrapping="Wrap"
                   Margin="0,6,0,0"
                   Text="{Binding GroupNameError}"
                   Visibility="{Binding GroupNameError, Converter={StaticResource NullToVisibilityConverter}}"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    IsCancel="True"
                    Margin="0,0,8,0"/>
            <Button Content="Create Group"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
