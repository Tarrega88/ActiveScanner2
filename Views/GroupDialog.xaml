<Window x:Class="ActiveScanner.Views.GroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ActiveScanner.Views"
        xmlns:converters="clr-namespace:ActiveScanner.Converters"
        xmlns:models="clr-namespace:ActiveScanner.Models"
        Height="700" Width="900"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Title>
        <Binding Path="IsEditMode">
            <Binding.Converter>
                <local:BoolToTitleConverter/>
            </Binding.Converter>
        </Binding>
    </Window.Title>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>
        
        <!-- Local converters -->
        <local:BoolToTitleConverter x:Key="BoolToTitleConverter"/>
        <local:BoolToSaveButtonTextConverter x:Key="BoolToSaveButtonTextConverter"/>
        <local:ObjectTypeToStringConverter x:Key="ObjectTypeToStringConverter"/>
        
        <Style x:Key="DomainButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Padding" Value="12,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: Group Name and Display Options -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Group Name:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="Medium"/>
            
            <StackPanel Grid.Column="1">
                <TextBox Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Enter a unique group name"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                <TextBlock Text="{Binding GroupNameError}" 
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                           FontSize="11" Margin="4,2,0,0"
                           Visibility="{Binding GroupNameError, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <TextBlock Text="Type:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox SelectedItem="{Binding GroupObjectType}"
                          MinWidth="110"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Object Type"
                          Padding="8,6">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource ObjectTypeToStringConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                    <models:GroupObjectType>None</models:GroupObjectType>
                    <models:GroupObjectType>Computers</models:GroupObjectType>
                    <models:GroupObjectType>Users</models:GroupObjectType>
                    <models:GroupObjectType>Printers</models:GroupObjectType>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Main Content: Two columns with resizable splitter -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1.5*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Folder Navigation -->
            <Border Grid.Column="0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,4,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <TextBlock Grid.Row="0" Text="Navigate to Folder" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

                    <!-- Domain Selection -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Domain:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="250"
                                  ItemsSource="{Binding PresetDomains}"
                                  SelectedItem="{Binding SelectedDomain}"
                                  DisplayMemberPath="Name"
                                  materialDesign:HintAssist.Hint="Select domain..."
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                    </StackPanel>

                    <!-- Breadcrumb Navigation -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8"
                                Visibility="{Binding Breadcrumbs.Count, Converter={StaticResource CountToVisibilityConverter}}">
                        <!-- Back Button -->
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Width="28" Height="28"
                                Padding="0"
                                ToolTip="Go back one level"
                                Command="{Binding GoBackCommand}"
                                IsEnabled="{Binding CanGoBack}">
                            <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18"/>
                        </Button>

                        <!-- Breadcrumb Path -->
                        <ItemsControl ItemsSource="{Binding Breadcrumbs}" VerticalAlignment="Center">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{Binding DisplayName}"
                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                Padding="8,4"
                                                MinWidth="0"
                                                Command="{Binding DataContext.NavigateToBreadcrumbCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                        <TextBlock Text="\" VerticalAlignment="Center" Margin="0"
                                                   Visibility="{Binding IsLast, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- Subfolders List -->
                    <ListBox Grid.Row="3" 
                             ItemsSource="{Binding Subfolders}"
                             SelectedItem="{Binding SelectedFolder}"
                             Style="{StaticResource MaterialDesignListBox}"
                             SelectionChanged="SubfoldersList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="4,2">
                                    <materialDesign:PackIcon Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,10,0">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Kind" Value="Folder"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="organizationalUnit">
                                                        <Setter Property="Kind" Value="FolderAccount"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="13"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Add to Group Button -->
                    <Button Grid.Row="4" 
                            Content="Add Current Folder to Group"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding AddCurrentToGroupCommand}"
                            Margin="0,12,0,0"
                            HorizontalAlignment="Stretch"
                            IsEnabled="{Binding CurrentPath, Converter={StaticResource StringToBoolConverter}}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Add Current Folder to Group"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </Grid>
            </Border>

            <!-- Resizable Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="Transparent" Cursor="SizeWE"/>

            <!-- Right: Group Paths -->
            <Border Grid.Column="2" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="4,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="Paths in Group" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="{Binding GroupPaths.Count, StringFormat='{}{0} path(s)'}" 
                                   FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </StackPanel>

                    <!-- Paths List -->
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding GroupPaths}"
                             SelectedItem="{Binding SelectedGroupPath}">
                        <!-- Style with empty state trigger -->
                        <ListBox.Style>
                            <Style TargetType="ListBox" BasedOn="{StaticResource MaterialDesignListBox}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding GroupPaths.Count}" Value="0">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Border Background="{DynamicResource MaterialDesignCardBackground}" 
                                                            BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                                            BorderThickness="1" CornerRadius="4">
                                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16">
                                                            <materialDesign:PackIcon Kind="FolderPlusOutline" Width="48" Height="48" 
                                                                                      HorizontalAlignment="Center"
                                                                                      Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                            <TextBlock Text="No paths added yet" 
                                                                       HorizontalAlignment="Center" Margin="0,8,0,0"
                                                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                            <TextBlock Text="Navigate to a folder and click 'Add Current Folder'" 
                                                                       HorizontalAlignment="Center" FontSize="11"
                                                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.Style>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayPath}" FontWeight="Medium" 
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding DistinguishedName}"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" 
                                            Style="{StaticResource MaterialDesignIconButton}"
                                            Width="28" Height="28"
                                            ToolTip="Remove from group"
                                            Command="{Binding DataContext.RemovePathFromGroupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="4" Padding="12,8" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ProgressBar Grid.Column="0" IsIndeterminate="True" Width="24" Height="24"
                             Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Margin="0,0,12,0"/>
                
                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}" 
                    Margin="0,0,8,0"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding IsEditMode, Converter={StaticResource BoolToSaveButtonTextConverter}}"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</Window>
