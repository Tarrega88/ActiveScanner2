{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ActiveScanner",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/ActiveScanner.csproj"
        }
    ]
}
